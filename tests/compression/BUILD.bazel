load("@rules_img//img:layer.bzl", "image_layer")
load(":defs.bzl", "layer_combinations")

image_layer(
    name = "large_1GB",
    srcs = {
        "/1GB.bin": "@large_file_1GB//file",
    },
    tags = [
        "exclusive",
        "manual",
        "no-cache",
    ],
)

image_layer(
    name = "large_10GB",
    srcs = {
        "/10GB.bin": "@large_file_10GB//file",
    },
    tags = [
        "exclusive",
        "manual",
        "no-cache",
    ],
)

layer_combinations()
