load("@bazel_skylib//rules:common_settings.bzl", "string_flag")

string_flag(
    name = "push_strategy",
    build_setting_default = "upload",
    values = [
        "upload",
        "cas",
    ],
    visibility = ["//img/private:__subpackages__"],
)
